import XCTest
@testable import OpenPermissionSettingsPlugin

final class OpenPermissionSettingsPluginTests: XCTestCase {
    func testExample() throws {
        let plugin = OpenPermissionSettingsPlugin()
    }
}
